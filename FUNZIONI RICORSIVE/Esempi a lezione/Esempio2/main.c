/*calcolo il minimo di una sequenza di elementi [a1, a2,..,a3]   */

#include <stdlib.h>
#include <stdio.h>
#include <time.h>
/*
int CalcoloMinSeq(int* sif, int start, int finish)
{
	int i = (start + finish) / 2;
	if(start + finish == 1)
	{ 
		if()
	}
	else
	{

	}
}


int main()
{
	int seq[100];
	for (int i = 0; i < 100; i++)
	{
		int pos = rand() % 100;
		seq[i] = pos;
		printf("%d/n", seq[i]);
	}

	int ris = CalcoloMinSeq(seq,0,4);
	return 0;
}
DA RIGUARDARE TUTTO*/